# Pinned environment for graphgen-toolgrad.
#
# Measured green: Python 3.12.3, via
#   <venv>/bin/python bridge/tests/run_tests.py
# from the repo root. No API keys, no Ray needed for the suite.
#
# Both integration forks install from git (pinned SHAs); `pip install -e .`
# pulls them automatically via pyproject.toml.
networkx==3.6.1                # core dep: KG context export, ToolKG
toolgrad @ git+https://github.com/Srinivasoo7/toolgrad@f4aaff100a4afcc2d0ce6e50438cc47b5a0929cc
graphg @ git+https://github.com/Srinivasoo7/GraphGen@0fa7c7ebc92d458231dc5687f00771f78842516c
langchain-core==1.6.3          # (via toolgrad) ChatPromptTemplate objects
langchain-mcp-adapters==0.3.2  # (via toolgrad) live MCP tool discovery
